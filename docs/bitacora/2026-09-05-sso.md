# 2026-09-05 — LXC sso (Authentik)

Nuevo environment `environments/sso/`, mismo módulo `lxc` que `plane`/`observability`/
`vault`. LXC 106, `192.168.8.94/24`, 4 cores/4GB/20GB, `nesting=true` (Docker Compose
adentro). `tofu plan`/`apply` limpios, sin incidentes propios de Terraform/Proxmox —
el detalle de Authentik en sí (versión de imagen, secretos, el bug de
`openssl rand -base64` cortando el `.env`) vive en el repo
[`sso`](https://github.com/gaelsg/sso), no acá.
