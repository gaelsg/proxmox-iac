# 2026-09-05 — LXC pki (step-ca)

Nuevo environment `environments/pki/`, mismo módulo `lxc` que `sso`/`plane`/
`observability`. LXC 107, `192.168.8.95/24`, 2 cores/1GB/10GB, `nesting=true`
(Docker). `tofu plan`/`apply` limpios, sin incidentes propios de Terraform/Proxmox
— el detalle de `step-ca` en sí vive en el repo [`pki-ca`](https://github.com/gaelsg/pki-ca).
